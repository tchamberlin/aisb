ARG BASE_IMAGE=localhost/aisb-base:latest
FROM ${BASE_IMAGE}

ARG PI_VERSION=unknown
RUN echo "pi-coding-agent cache key: ${PI_VERSION}" \
 && npm install -g @mariozechner/pi-coding-agent@latest

ENTRYPOINT ["pi"]
